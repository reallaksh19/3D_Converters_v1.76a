export const XML_COMPARE_CANVAS_LEVEL_SELECTOR_SCHEMA='xml-compare-canvas-level-selector/v1';
const esc=(v)=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
function option(value,label,selected){return `<option value="${esc(value)}" ${value===selected?'selected':''}>${esc(label)}</option>`;}
function levelOptions(levels=[],selected=''){return levels.map((level)=>option(level.levelId,`${level.label} (${level.recordCount})`,selected)).join('');}
function fieldOptions(fields=[],selected=''){return fields.map((field)=>option(field,field,selected)).join('');}
export function renderXmlCompareCanvasLevelSelectorHtml({side='source',levelView=null}={}){const profile=levelView?.hierarchyProfile||{levels:[]},levels=profile.levels||[],fields=levelView?.fields||[];if(!levels.length)return '<div class="xml-compare-level-selector is-empty">No hierarchy levels detected</div>';return `<div class="xml-compare-level-selector" data-xml-compare-level-selector="${esc(side)}"><label>View Level<select data-xml-compare-level-select="levelId" data-xml-compare-side="${esc(side)}">${levelOptions(levels,levelView?.levelId)}</select></label><label>Display By<select data-xml-compare-level-select="displayField" data-xml-compare-side="${esc(side)}">${fieldOptions(fields,levelView?.displayField)}</select></label><label>Key Field<select data-xml-compare-level-select="keyField" data-xml-compare-side="${esc(side)}">${fieldOptions(fields,levelView?.keyField)}</select></label></div>`;}
export const _test=Object.freeze({esc,option,levelOptions,fieldOptions});
