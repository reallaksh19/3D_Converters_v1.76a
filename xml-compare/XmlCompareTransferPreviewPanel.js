export const XML_COMPARE_TRANSFER_PREVIEW_PANEL_SCHEMA='xml-compare-transfer-preview-panel/v2';
const esc=(v)=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
function compact(value={}){if(!value||typeof value!=='object')return '';return Object.entries(value).slice(0,6).map(([key,val])=>`${key}=${val}`).join('; ');}
function table(rows=[]){if(!rows.length)return '<div class="xml-compare-empty">No field preview. Accept or create a manual connector first.</div>';return `<div class="xml-compare-table-scroll"><table class="xml-compare-topology-table"><thead><tr><th>Field</th><th>Source A</th><th>Target B</th><th>Status</th></tr></thead><tbody>${rows.slice(0,25).map((item)=>`<tr><td>${esc(item.field)}</td><td>${esc(item.sourceValue)}</td><td>${esc(item.targetValue||'—')}</td><td>${esc(item.status)}</td></tr>`).join('')}</tbody></table></div>`;}
function nodeBlockTable(blocks=[],diagnostics=[]){if(blocks.length)return `<div class="xml-compare-table-scroll"><table class="xml-compare-topology-table"><thead><tr><th>Node-owned block</th><th>Items</th><th>Fields</th><th>Source refs</th></tr></thead><tbody>${blocks.slice(0,40).map((block)=>`<tr><td>${esc(block.blockName)}</td><td>${esc(block.itemCount)}</td><td>${esc(compact(block.compactFields||block.fields))}</td><td>${esc((block.sourceRefs||[]).join(' | '))}</td></tr>`).join('')}</tbody></table></div>`;const message=diagnostics.find((item)=>item.code==='node_owned_blocks_not_found')?.message||'No node-owned blocks found for selected node identity.';return `<div class="xml-compare-empty">${esc(message)}</div>`;}
export function renderXmlCompareTransferPreviewPanelHtml(preview=null){const rows=Array.isArray(preview?.rows)?preview.rows:[],blocks=Array.isArray(preview?.nodeOwnedBlocks)?preview.nodeOwnedBlocks:[],diagnostics=Array.isArray(preview?.diagnostics)?preview.diagnostics:[],summary=preview?.summary||{};return `<section class="xml-compare-bottom-panel" data-xml-compare-transfer-preview><header>Transfer Preview</header><div class="xml-compare-detail-card"><div><b>Manual links</b><span>${esc(summary.connectorCount||0)}</span></div><div><b>Preview fields</b><span>${esc(summary.previewCount||0)}</span></div><div><b>Node-owned blocks</b><span>${esc(summary.nodeOwnedBlockCount||0)}</span></div><div><b>Mode</b><span>preview only</span></div></div><section class="xml-compare-transfer-node-blocks"><header>Node-owned blocks</header>${nodeBlockTable(blocks,diagnostics)}</section>${table(rows)}</section>`;}
export const _test=Object.freeze({esc,compact,table,nodeBlockTable});
